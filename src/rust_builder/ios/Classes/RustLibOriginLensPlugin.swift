import Flutter
import UIKit

public class RustLibOriginLensPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {
    // This is an FFI plugin, no method channel needed
  }
}
